# Modular Platform Security Standard

This standard applies to every production component of the modular records platform.

## Messaging

Every records platform must have an SQS queue.

## Storage

Every S3 bucket that stores records must have versioning enabled.
